#include "main.h"

/**
 * print_last_digit - prints last digit of a number
 * @n: The last digit gangan
 *
 * Return: Value of the last gigit.
 */

int print_last_digit(int n)
{
	int last_digit = n % 10;

	if (last_digit < 0)
		last__digit *= -1;

	_putchar((last_digit + '0');

	return (last_digit);

}

